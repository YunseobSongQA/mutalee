name = "mutalee"
compatibility_date = "2024-09-23"
pages_build_output_dir = "."

# 구독 저장은 worker/cron.js의 Durable Object가 담당하므로 KV 바인딩은 더 이상 없다.
# functions/api/*는 SYNC_TOKEN 시크릿(Pages 환경 변수)으로 Worker에 전달만 한다.
